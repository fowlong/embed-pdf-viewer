name: Build Snippet

on:
  push:
    branches:
      - main
    paths:
      - 'snippet/**'
  pull_request:
    paths:
      - 'snippet/**'

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./snippet

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: 'snippet/pnpm-lock.yaml'

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build snippet
        run: pnpm run build

      - name: Verify build output
        run: ls -l dist
